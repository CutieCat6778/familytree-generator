services:
  familytree-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3005"
    volumes:
      - ./data:/data:ro
      - ./web/dist:/web/dist:ro
    command:
      - ./familytree-server
      - -port
      - "3005"
      - -data
      - /data
